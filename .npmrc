# NPM Workspace Configuration for Monorepo
# Ensures proper package hoisting to prevent React instance duplication

# Hoist all packages to root node_modules
hoist=true

# Ensure single version of React across all workspaces
public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom

# Strict peer dependencies handling
legacy-peer-deps=false
